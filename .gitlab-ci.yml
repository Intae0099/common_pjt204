# 🚀 AI 애플리케이션 Windows 로컬 배포

stages:
  - deploy

variables:
  GIT_STRATEGY: clone
  GIT_DEPTH: 1

# 📦 Windows 로컬 자동 배포
deploy:
  stage: deploy
  before_script:
    # AI 프로젝트 폴더로 이동
    - Set-Location ai
    
    # 로컬 config/.env 파일을 빌드 디렉토리로 복사
    - 'if (Test-Path "F:\S13P11B204\ai\config\.env") { Copy-Item "F:\S13P11B204\ai\config\.env" "config\.env" -Force; Write-Host "✅ config/.env 복사 완료" } else { Write-Host "❌ 로컬 config/.env 파일 없음"; exit 1 }'
    
    # 환경변수 파일 확인
    - 'if (Test-Path "config\.env") { Write-Host "✅ config/.env 파일 존재" } else { Write-Host "❌ config/.env 파일 없음"; exit 1 }'
    
  script:
    # 1️⃣ 현재 위치 확인
    - 'Write-Host "현재 작업 디렉토리: $(Get-Location)"'
    - 'Write-Host "파일 목록:"'
    - Get-ChildItem
    
    # 2️⃣ Docker 상태 확인
    - docker --version
    - docker-compose --version
    
    # 3️⃣ 기존 컨테이너 정리
    - Set-Location docker
    - docker-compose down
    
    # 4️⃣ 새 빌드 및 배포
    - docker-compose up -d --build
    
    # 5️⃣ 배포 확인
    - Start-Sleep 20
    - 'try { Invoke-RestMethod -Uri "http://localhost:8997/" -Method Get } catch { Write-Host "❌ 배포 실패"; exit 1 }'
    - 'Write-Host "✅ 배포 성공!"'
    
    # 6️⃣ 상태 확인
    - docker-compose ps
    - docker-compose logs --tail=50 ai-app
    
  only:
    - dev-AI
    - feature/S13P11B204-488
  allow_failure: false